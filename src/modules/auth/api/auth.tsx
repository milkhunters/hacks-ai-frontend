import { SignInCredentials, SignUpCredentials } from '../types/credentials';

export const signIn = async (data: SignInCredentials) => {
	return data;
};

export const signUp = async (data: SignUpCredentials) => {
	return data;
};

export const signOut = async () => {
	return null;
};
